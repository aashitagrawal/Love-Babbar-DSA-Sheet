#include <bits/stdc++.h>
using namespace std;

// Today's Date - 8 Jan 2024
// GFG Question Name - Permutations of a given string

class Solution
{
public:
    vector<string> find_permutation(string S)
    {
        for(int level=0; level<S.size(); ++level)
        {
            
        }
    }
};

int main()
{
    string s = "ABGS";
    Solution solution;
    vector<string> ans = solution.find_permutation(s);
    for (auto &ele : ans)
        cout << ele << " ";
    cout << endl;
    return 0;
}